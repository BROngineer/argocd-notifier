argocd-notifier has been installed.

1. Check status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. Wire it up to ArgoCD's notifications-engine — see:
   https://github.com/BROngineer/argocd-notifier/blob/main/docs/setup.md
{{- if .Values.leaderElection.enabled }}

3. Leader election is enabled. Every pod should be Ready (non-leaders
   forward requests to the leader rather than failing readiness — see
   docs/design.md#high-availability). Confirm exactly one pod holds the
   lease:
   kubectl get lease {{ .Values.leaderElection.leaseName }} -n {{ include "argocd-notifier.leaderElectionNamespace" . }} \
     -o jsonpath='{.spec.holderIdentity}{"\n"}'
{{- end }}
